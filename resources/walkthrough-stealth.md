## 隐身模式

适合在办公环境使用。

开启后：
- Webview 标题显示为 `README.md`，看起来像代码文件
- 帖子作者和评论者的头像隐藏
- 评论点赞数隐藏
- 帖子列表中的点赞/评论数隐藏

在设置中搜索 `heybox.stealthMode` 并设为 `true` 即可启用。

也可同时开启简约模式 `heybox.minimalMode: true`，隐藏列表中的社交数据。